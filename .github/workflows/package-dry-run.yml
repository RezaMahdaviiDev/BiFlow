name: Package dry-run

on:
  workflow_dispatch:
    inputs:
      platform:
        description: Platform to build (all, linux, windows)
        required: true
        default: all
        type: choice
        options:
          - all
          - linux
          - windows

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

# Non-publishing native packaging smoke test. Uploads workflow artifacts only;
# never creates or updates a GitHub Release.
# Job-level `if` cannot use the matrix context. Select platforms here, then
# expand the native OS matrix with fromJSON.

jobs:
  select:
    runs-on: ubuntu-24.04
    outputs:
      include: ${{ steps.select.outputs.include }}
    steps:
      - id: select
        env:
          PLATFORM: ${{ inputs.platform }}
        run: |
          set -euo pipefail
          linux='{"platform":"ubuntu-24.04","args":"--bundles deb,appimage --config {\"bundle\":{\"createUpdaterArtifacts\":false}}"}'
          windows='{"platform":"windows-2025","args":"--bundles nsis --config {\"bundle\":{\"createUpdaterArtifacts\":false}}"}'
          case "${PLATFORM}" in
            linux) include="[${linux}]" ;;
            windows) include="[${windows}]" ;;
            *) include="[${linux},${windows}]" ;;
          esac
          echo "include=${include}" >> "${GITHUB_OUTPUT}"

  build:
    needs: select
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.select.outputs.include) }}
    runs-on: ${{ matrix.platform }}
    timeout-minutes: 90
    steps:
      - name: Disable CRLF conversion on Windows
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.autocrlf false
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.0.1
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - uses: dtolnay/rust-toolchain@1.88.0
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: ". -> target"
      - name: Install Linux desktop dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xdg-utils libfuse2t64
      - name: Prefetch AppImage tools
        if: runner.os == 'Linux'
        run: ./scripts/prefetch-appimage-tools.sh
      - name: Install NSIS
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install nsis -y --no-progress
          $nsisDirs = @(
            "${env:ProgramFiles(x86)}\NSIS",
            "${env:ProgramFiles}\NSIS"
          )
          foreach ($nsis in $nsisDirs) {
            if (Test-Path $nsis) {
              Add-Content -Path $env:GITHUB_PATH -Value $nsis
              $env:Path = "$nsis;$env:Path"
            }
          }
          Get-Command makensis -ErrorAction Stop
      - run: pnpm install --frozen-lockfile
      - name: Stage privileged helper
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            ./scripts/stage-helper.sh windows
          else
            ./scripts/stage-helper.sh
          fi
      - uses: tauri-apps/tauri-action@v1
        with:
          args: ${{ matrix.args }}
      - name: Stage portable Windows app
        if: runner.os == 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          exe="target/release/iran-split-desktop.exe"
          test -f "${exe}"
          cp "${exe}" BiFlow.exe
      - name: Upload Linux dry-run artifacts
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: dry-run-linux
          path: |
            target/release/bundle/deb/*.deb
            target/release/bundle/appimage/*.AppImage
          if-no-files-found: error
          retention-days: 3
      - name: Upload Windows dry-run artifacts
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: dry-run-windows
          path: |
            BiFlow.exe
            target/release/bundle/nsis/*-setup.exe
          if-no-files-found: error
          retention-days: 3
