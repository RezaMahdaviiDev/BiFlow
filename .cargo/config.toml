[target.x86_64-pc-windows-msvc]
# Link the MSVC C runtime statically so shipped binaries (iran-split-helper.exe,
# the Tauri app) run on machines without the VC++ Redistributable installed.
rustflags = ["-C", "target-feature=+crt-static"]
